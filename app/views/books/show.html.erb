<div>
  <%= flash[:notice] %>
</div>
<div class="container">
  <div class ="row">
    <div class= "col-md-3">
     <h2>User info</h2>
  <table class = "table">
    <tr>
      <th><%= image_tag @user.get_profile_image(100,100) %></th>
    </tr>
    <tr>
      <th>name</th><th><%= @user.name %></th>
    </tr>
    <tr>
      <th>introduction</th><th><%= @user.introduction %></th>
    </tr>
  </table> 
  <a class="btn btn-outline-secondary btn-block" href="<%= edit_user_path(@user.id) %>">
    <i class="fas fa-user-cog"></i>
    </a>
       <%#= link_to "編集", edit_user_path(@user.id) %>
  
      <h2>New book</h2>
       <%= form_with model: @book do |f| %>
       <h4>Title</h4>
         <%= f.text_field :title %>
       <h4>Opinion</h4>
       <%= f.text_area :body %>
       <%= f.submit 'Create Book' %>
      <% end %>
    </div>

  <div class="col-md-8 offset-md-1">
    <h2>Book detail</h2>
      <table class = "table">

   <%# @books.each do |book| %>
   <tr>
      <th>
          <%= link_to user_path(@books.user.id) do %>
          <%= image_tag @user.get_profile_image(100,100) %><br>
          <% end %>
          <%= link_to @user.name,user_path(@books.user.id) %>
      </th>
     <th><%= link_to @books.title,book_path(@books.id) %></th>
     <th><%= @books.body %></th>
     <th><%= link_to "Edit", edit_book_path(@books.id) if @books.user == current_user %></th>
     <th><%= link_to "Destroy", book_path(@books.id), method: :delete, "data-confirm" => "Are you sure?" if @books.user == current_user %></th>
   </tr>
  <%# end %>
  </table>
   </div>
  </div>
 </div>
  
 